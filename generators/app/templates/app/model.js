/**
 * <%= properCasedModel %> business logic
 */

export default class <%= properCasedModel %> {
  constructor({ application_id, user_id, attribute_3 }) {
    Object.assign(this, { application_id, user_id, attribute_3 });
  }
};